sudo: required
services:
  - docker

before_install:
  - docker build -t shon3005/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run shon3005/react-test npm test -- --coverage

after_success:
  - docker build -t shon3005/multi-client ./client
  - docker build -t shon3005/multi-nginx ./nginx
  - docker build -t shon3005/multi-server ./server
  - docker build -t shon3005/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push shon3005/multi-client
  - docker push shon3005/multi-nginx
  - docker push shon3005/multi-server
  - docker push shon3005/multi-worker
